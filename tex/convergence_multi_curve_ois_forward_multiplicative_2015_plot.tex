\begin{center}
  \begin{minipage}{\linewidth}
    \begin{tikzpicture}
      \begin{axis}[smooth, thick,cycle list name=plots,
          width=0.98\linewidth,height=220pt,
          ymajorgrids={true},minor y tick num={1},yminorgrids={true},
          grid style={gray!30},
          xlabel={Time [years]},xlabel style={yshift=-0.4cm,xshift=1cm},
          ylabel={Forward Rate [\%]},
          legend style={at={(0.5,-0.25)},anchor=north,draw=none,legend columns=4}, % Put the legend below the plot
          xtick align=outside,
          xtick pos=left,
          ytick align=outside,
          ytick pos=left,
          xmin = 0,
          ymin = -0.2,
          ymax = 0.03,
          x label style={rotate=0,anchor=east} % Display labels sideways
        ]

        \addplot+[only marks,error bars/.cd,y dir=both,y explicit,error bar style={line width=0.5pt}][smooth] 
        table[x=Time,y=OIS Forward Rate Multi-Curve,y error=OIS Forward Rate Multi-Curve Error,col sep=tab] {results/pricing_2015_convergence/1000_multiplicative_1/initial_calibration_results.csv};

\addplot+[only marks,error bars/.cd,y dir=both,y explicit,error bar style={line width=0.5pt}][smooth] 
        table[x=Time,y=OIS Spot Rate Single-Curve,y error=OIS Spot Rate Single-Curve Error,col sep=tab] {results/pricing_2015_convergence/1000_multiplicative_1/initial_calibration_results.csv};

\addplot+[only marks,error bars/.cd,y dir=both,y explicit,error bar style={line width=0.5pt}][smooth] 
        table[x=Time,y=OIS Spot Rate Single-Curve,y error=OIS Spot Rate Single-Curve Error,col sep=tab] {results/pricing_2015_convergence/10000_multiplicative_1/initial_calibration_results.csv};

\addplot+[only marks,error bars/.cd,y dir=both,y explicit,error bar style={line width=0.5pt}][smooth] 
        table[x=Time,y=OIS Spot Rate Single-Curve,y error=OIS Spot Rate Single-Curve Error,col sep=tab] {results/pricing_2015_convergence/100000_multiplicative_1/initial_calibration_results.csv};

\addplot+[only marks,error bars/.cd,y dir=both,y explicit,error bar style={line width=0.5pt}][smooth] 
        table[x=Time,y=OIS Spot Rate Single-Curve,y error=OIS Spot Rate Single-Curve Error,col sep=tab] {results/pricing_2015_convergence/1000000_multiplicative_1/initial_calibration_results.csv};


        \legend{ Reference,1x1000,1x10000,1x100000,10x100000 }
      \end{axis}
    \end{tikzpicture}
    \captionof{figure}{Valuation of IBOR forward rates in the multiplicative model with different amounts of Monte-Carlo paths.}
  \end{minipage}
\end{center}

